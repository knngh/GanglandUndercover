# Gangland Undercover M7 Closeout

- Date: 2026-06-04 20:41 CST
- Project: `/Users/zhugehao/projects/GanglandUndercover`
- Unity: 6000.4.5f1
- Scope: compile error fix and M7 handoff state

## Result

M7 is closed for the C# compile surface. Runtime, test, and editor assemblies compile with no errors.

This does not prove the full Relay runtime loop yet. True two-machine Relay, Host disconnect/migration, Canvas map mounting, and first-session usability still need runtime validation in Unity after the current Editor state is ready for playtesting.

## Compile Verification

| Target | Result | Notes |
|---|---|---|
| `Assembly-CSharp.rsp` | Pass | 0 errors; 4 Netcode obsolete warnings for `RequireOwnership=false`; 1 existing `voiceJoinInProgress` unused warning |
| `GanglandUndercover.Tests.rsp` | Pass | No compile errors |
| `Assembly-CSharp-Editor.rsp` | Pass | No compile errors |

Unity batchmode was not used for this pass because the project may already be open in a Unity Editor instance. The compile check used Unity 6000.4.5f1's bundled Roslyn compiler and Bee response files.

## Compile Fixes

| File | Fix |
|---|---|
| `Assets/_Project/Scripts/Online/Map/CanvasMapView.cs` | Restored the missing `DrawBodyMarkers()` method boundary and removed the stale `GetPlayerMarkerColor` overload that referenced invalid role data. |
| `Assets/_Project/Scripts/Online/OnlineMatchController.cs` | Merged duplicate M7 Relay/Lobby API declarations and kept compatible Relay request entrypoints for both HUD and Lobby UI. |
| `Assets/_Project/Scripts/Gameplay/PrototypeBootstrap.cs` | Passed the active `OnlineMatchController` into `LobbyController.Initialize(...)` after the M7 signature change. |
| `Assets/_Project/Scripts/Online/Map/CanvasMapView.cs` | Aligned room UI rendering with the actual `ShipRoomSpec` fields: `Label` and `Floor`. |
| `Assets/_Project/Scripts/Online/Map/KenneySpriteDecorator.cs` | Aligned Kenney room matching/naming with `ShipRoomSpec.Label`. |
| `Assets/_Project/Scripts/Online/HostMigrationManager.cs` | Wired `useFallbackOnMigrationFail` into the migration fallback path instead of leaving it as an inert inspector field. |

## M7 Status

| Area | Status |
|---|---|
| Relay room code API | Compiles with event hooks for status, room code, and connection changes; `RequestRelayClient(string)` now feeds the existing Relay join path. |
| Lobby UI integration | `PrototypeBootstrap` now supplies `LobbyController` with the online match controller, so create/join calls compile against the real Relay API. |
| Host migration fallback | Compile-ready fallback path exists for insufficient players, failed election, and migration timeout. Runtime behavior still needs forced Host-disconnect validation. |
| Canvas map UI | Compile-ready static rooms, task markers, player markers, and body markers use `OnlineMapService` data. Mounting/initialization in the live HUD still needs Unity runtime validation. |
| First-session path | Main menu to Lobby create/join path is compile-ready. New-player usability still requires playtest validation. |

## Remaining Before M8

1. Run Unity Test Runner for `GanglandUndercover.Tests` in the Editor, not only the Roslyn `.rsp` compile check.
2. Run true two-machine Relay: create room, share room code, join, ready/start, complete one full match.
3. Force Host disconnect in a two/three-client session and verify either successful migration or clean fallback result with no stuck clients.
4. Mount or verify `CanvasMapView.Initialize(controller, mapService)` in the live HUD/scene path and test minimap/full-map visibility, zoom, pan, and marker positions.
5. Run a no-doc first-session usability pass: main menu, lobby, room code, start match, task, meeting, result.
6. Migrate the remaining Netcode `ServerRpc(RequireOwnership=false)` calls to the newer `[Rpc]` API before release hardening.
