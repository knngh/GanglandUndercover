# Gangland Undercover M5-M6 Closeout

- Date: 2026-06-04 19:20 CST
- Project: `/Users/zhugehao/projects/GanglandUndercover`
- Unity: 6000.4.5f1
- Scope: compile error fix and M5-M6 handoff state

## Result

M5-M6 are closed for the C# compile surface. Runtime, test, and editor assemblies compile with no errors.

This does not mean the M5/M6 runtime loop is fully proven. Unity Test Runner, Host/Client double-open smoke, M5 minigame end-to-end, M5 surveillance viewing, and M6 multiplayer greybox playtests still need to run after the active Unity Editor instance is closed.

## Compile Verification

| Target | Result | Notes |
|---|---|---|
| `Assembly-CSharp.rsp` | Pass | 0 errors; 4 Netcode obsolete warnings for `RequireOwnership=false`; 1 existing `voiceJoinInProgress` unused warning |
| `GanglandUndercover.Tests.rsp` | Pass | No compile errors |
| `Assembly-CSharp-Editor.rsp` | Pass | No compile errors |

Unity batchmode was not used for this pass because the project is already open in a Unity Editor instance. The compile check used Unity 6000.4.5f1's bundled Roslyn compiler and Bee response files.

## Compile Fixes

| File | Fix |
|---|---|
| `Assets/_Project/Scripts/Online/Map/MapValidator.cs` | Converted BFS max-distance steps to `int` with `Mathf.CeilToInt`. |
| `Assets/_Project/Scripts/Online/OnlineMatchController.cs` | Changed M5 task completion evidence writes to `taskService.AddEvidence(gain, status)` and fixed `gain` scope for evidence-board logging. |
| `Assets/_Project/Scripts/Online/OnlineMatchController.cs` | Replaced invalid `IsServer` access in surveillance spawning with `networkManager != null && networkManager.IsServer`. |
| `Assets/_Project/Scripts/Online/Surveillance/OnlineSecurityCamera.cs` | Renamed ClientRpc target helper to avoid hiding `NetworkBehaviour.RpcTarget`. |

## M5 Status

| Area | Status |
|---|---|
| Minigame protocol | `OnlineMiniGameBridge` compiles with server validation, client start/submit flow, task completion, repair completion, and ClientRpc feedback. |
| Minigame coverage | Factory maps task ids across 13 buckets, including Wire/Keypad/Swipe/Scan/Download/Memory/Sort/Tap/Asteroid/Calibrate/EvidenceArchive plus rotations. |
| Repair loop | `ValidateRepairStart` and `ValidateAndRepairTask` compile and route through repair minigame submission. |
| Evidence board/suspicion | Task completion logs task trail and evidence-chain entries, and lowers suspicion on completed tasks. |
| Surveillance | `OnlineSecurityCamera` compiles, only serializes lightweight in-zone player data, and is ticked from the host simulation after runtime camera NetworkObjects are created. Viewing UI and double-open behavior still need runtime validation. |

## M6 Status

| Area | Status |
|---|---|
| Greybox data | `MapLayoutData` compiles as the design-space source for rooms, corridors, tasks, vents, cameras, and blockers. |
| Greybox builder | `GreyboxMapBuilder` compiles and can be toggled through `useGreyboxMode` when `mapLayoutData` is assigned. |
| Map validator | `MapValidator` compiles with reachability, task distribution, vent connectivity, surveillance coverage, sight-blocker, and room-connectivity checks. |
| Surveillance zones | `OnlineMapService.SurveillanceZones()` provides 6 design-space camera zones. |
| Test/art plan | `output/m6_playtest_and_art_strategy.md` defines M6.2 playtest targets and M6.3 tile/sprite replacement strategy. |

## Remaining Before M7

1. Close the active Unity Editor instance, then run Unity Test Runner for `GanglandUndercover.Tests`.
2. Run local Host/Client smoke for M5: open task minigame, complete task, trigger sabotage, repair task, enter meeting, verify evidence board.
3. Verify surveillance runtime refresh in Host/Client: confirm `OnlineSecurityCamera.ServerTick()` updates viewers and clients only receive in-zone player data.
4. Run M6.2 greybox playtests using `useGreyboxMode=true` and a real `MapLayoutData` asset.
5. Treat M6.3 80% tile/sprite replacement as pending production work; the current state is strategy plus compile-ready greybox tooling.
6. Migrate Netcode `ServerRpc(RequireOwnership=false)` usages to the new `[Rpc]` API before release hardening.
