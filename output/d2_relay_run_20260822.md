# D-2 Relay 回归执行记录（2026-08-22 至 2026-08-24）

## 本轮目标

按 Demo D-2 计划重新执行 Relay `normal`、`migration` 和
`migration-threeclient` 分进程门禁，并为后续 4/6/8/10 人实战采样建立可追溯记录。

本记录已完成 Relay 自动化门禁，包括合法摄像头 watcher 与非空远端数据回调；4/6/8/10 人真实节奏局、摄像头远端画面目视确认和双人真人视角走查仍未完成。

## 执行结果

| 场景 | 命令 | 结果 | 证据 |
| --- | --- | --- | --- |
| normal（沙箱权限） | `GANGLAND_RELAY_TIMEOUT_SECONDS=300 bash run-relay-twoprocess.sh` | **BLOCKED**：Host 在启动 UPM 时因 `listen EPERM /tmp/Unity-Upm-*.sock` 退出；未写 Relay code，未启动 Client | `Logs/relay-host.log`（本轮覆盖） |
| normal（本机权限） | `GANGLAND_RELAY_TIMEOUT_SECONDS=300 bash run-relay-twoprocess.sh` | **PASS**：Host/Client exit 0，22 秒写码，`connectedClients=2`，三类恶意注入均拒绝 | `Logs/relay-host-results.xml`、clone `relay-client-results.xml`；最终 joinCode=`9CBLHT` |
| normal + 合法摄像头数据（D-2-R2） | `GANGLAND_RELAY_SCENARIO=normal GANGLAND_RELAY_TIMEOUT_SECONDS=420 bash run-relay-twoprocess.sh` | **PASS**：Host/Client exit 0；恶意摄像头请求仍被拒；合法 watcher=1；Client 收到 1 次非空 `VisiblePlayerData[]` | `Logs/relay-host-results.xml`、clone `relay-client-results.xml`、`/tmp/gangland-relay-code.txt.camera-data-received`；joinCode=`FKWBGK` |
| migration 双进程（采样 1） | `GANGLAND_RELAY_SCENARIO=migration ...` | **FLAKY FAIL**：candidate PASS，但旧 Host 重连后 60 帧内断开 | `relay-host-results.xml` failed；client PASS；oldCode=`MWPFHP` |
| migration 双进程（采样 2） | `GANGLAND_RELAY_SCENARIO=migration ...` | **PASS**：Host/Client exit 0，旧 Host 重连稳定，replacement `connectedClients=2` | `relay-migration-*.result.txt`；oldCode=`GMRLJP`、migrationCode=`PJBGCP` |
| migration 三进程（修正前） | `GANGLAND_RELAY_SCENARIO=migration-threeclient ...` | **FAIL**：身份 ID 假设把任务请求发给错误远端；同时暴露 observer 收尾/断线时序问题 | Host/Client/Observer XML；oldCode=`MBTWK8`、migrationCode=`QB6FWH` |
| migration 三进程（最终） | `GANGLAND_RELAY_SCENARIO=migration-threeclient ...` | **PASS**：三端 exit 0；旧 Host 重连、远端任务 RPC、迁移后投票、observer 跟随均 PASS | `Logs/relay-migration-threeclient-host-results.xml`、两个 clone XML；oldCode=`M6DGGT`、migrationCode=`QRWQQP` |
| migration 三进程（独立 XML 复跑） | `GANGLAND_RELAY_SCENARIO=migration-threeclient ...` | **PASS**：再次三端 exit 0；QoS/Relay 服务恢复后可复现 | 同名 scenario-specific XML；oldCode=`M6DGGT`、migrationCode=`QRWQQP` |

## 早期阻塞信号（已解除）

- `LicenseClient-zhugehao` IPC channel 不存在或反复断开。
- 日志出现 `Licensing initialization failed`、`The connection with the Unity Licensing Client has been lost`。
- 早期两次启动没有 `relay-host-results.xml`、`relay-client-results.xml` 或结果 marker，不能计为测试失败，也不能计为 PASS；清理孤立 Licensing Client 后后续回归已生成完整 XML。

## 已有历史证据（仅作参考）

- normal 双进程：2026-08-22/23 PASS，见 `Logs/relay-host-results.xml` 和 clone `relay-client-results.xml`。
- migration 双/三进程：本轮已重新生成 2026-08-22 证据；历史 2026-07-13 记录仅作为对照，见 `output/test_coverage_matrix_20260624.md` PLAN23/25-C。

本轮已生成本日期的 Host/Client/Observer XML。migration 双进程出现 1/2 次时序抖动，需在后续里程碑重复采样；三进程最终门禁已通过，并在独立 XML 复跑中再次通过。最终三进程样本为 `M6DGGT → QRWQQP`。

## 2026-08-23 至 2026-08-24 门禁增量

- `DemoBotMatchPlayTests` 新增 4/6/8/10 人独立规模门禁，均 PASS；另新增身份简报 `Opening → Action` 可读性门禁，PASS。针对性 XML：`ci-logs/20260823_demo_all.xml`（6/6）。
- 为支持从 8 人切到 10 人的大厅矩阵，修正 `SetRoomMinPlayers` 先按旧最大人数截断、导致最大人数联动分支不可达的问题。
- Relay normal 流程新增合法摄像头观看证据：Host 在 Action 将远端玩家放入指定摄像头区域，并通过 `NetworkObjectId` 指示 Client 订阅同一 clone；Client 等待真实 `OnCameraDataReceived` 非空回调，Host 通过共享 marker 核对 watcher 数量。
- 2026-08-24 停止从 2026-08-23 遗留的孤立 `Unity.Licensing.Client` 后，最小 batchmode 在 1 秒内重新建立 IPC 并正常退出，随后 D-2-R2 进入真实测试逻辑。
- 首次真实运行揭示测试编排问题：Host/Client 在 6 个摄像头中各自无序选取对象，未必是同一 NetworkObject；改为 marker 传递精确 `cameraNetworkObjectId`。第二次运行已收到 `updates=1 nonEmpty=True`，又修正跨进程 marker 的布尔值为固定小写协议。最终运行 Host/Client 均 PASS。
- D-2-R2 已关闭。最终 marker 为 `updates=1 nonEmpty=true ready=phase=Action remoteId=1 cameraNetworkObjectId=3 cameraReady=true`。

## 环境恢复记录

1. 已确认本机许可证可用，`UnityLicensingClient` 能建立当前用户 IPC channel。
2. 最小 `Unity -batchmode -quit -projectPath ...` 已完成 Package Manager 初始化并 exit 0，证据为 `ci-logs/20260824_license_probe.log`。
3. normal 摄像头数据门禁已重跑并关闭；migration 双进程抖动仍按每里程碑重复采样策略保留。
