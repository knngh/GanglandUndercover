# Gangland Undercover M8 Closeout

- Date: 2026-06-04 21:21 CST
- Project: `/Users/zhugehao/projects/GanglandUndercover`
- Unity: 6000.4.5f1
- Scope: compile error fix and M8 handoff state

## Result

M8 is closed for the C# compile surface. Runtime, test, and editor assemblies compile with no errors.

This is not a balance sign-off. PoliceStation runtime map selection, two-map full-match play, upgraded Bot behavior, profession tuning, and the 20-match balance-data target still need runtime validation.

## Compile Verification

| Target | Result | Notes |
|---|---|---|
| `Assembly-CSharp.rsp` | Pass | 0 errors; 4 Netcode obsolete warnings for `RequireOwnership=false`; 1 existing `voiceJoinInProgress` unused warning |
| `GanglandUndercover.Tests.rsp` | Pass | No compile errors |
| `Assembly-CSharp-Editor.rsp` | Pass | No compile errors |

Unity batchmode was not used for this pass because the project may already be open in a Unity Editor instance. The compile check used Unity 6000.4.5f1's bundled Roslyn compiler and Bee response files.

## Compile Fixes

| File | Fix |
|---|---|
| `Assets/_Project/Scripts/Online/OnlineProfession.cs` | Added `UnityEngine` so `Tooltip` attributes compile outside files that already import Unity types. |
| `Assets/_Project/Scripts/Online/MatchStatsCollector.cs` | Converted the private dictionary helper from an invalid extension method inside a non-static class into a normal private helper. |
| `Assets/_Project/Scripts/Online/MatchStatsCollector.cs` | Replaced the invalid GUID interpolation slice with explicit `ToString("N").Substring(0, 6)`. |
| `Assets/_Project/Scripts/Online/Bots/OnlineBotController.cs` | Replaced stale `AddSuspicionToPlayer(...)` calls with the existing `OnlineMatchController.AddSuspicion(...)` API. |

## M8 Status

| Area | Status |
|---|---|
| PoliceStation map | `OnlineMapService.ActiveMapType` and PoliceStation room/surveillance data compile. Runtime map selection and full-match PoliceStation flow still need validation. |
| Profession set | `OnlineProfession`, `ProfessionAbility`, `AbilityType`, and `OnlineRuleSet.ProfessionAbilities` compile. Runtime ability balance still needs playtest data. |
| Bot upgrade | `OnlineBotController` compiles with task/repair progress, vent use, profession ability use, and suspicion-based voting hooks. Host-only behavior still needs local and Relay playtests. |
| Match stats | `MatchStatsCollector` and `Assets/Editor/MatchStatsWindow.cs` compile, and the tuning guide exists at `output/m8_balance_tuning_guide.md`. Real logs and balance recommendations still need match data. |

## Remaining Before M9

1. Run Unity Test Runner for `GanglandUndercover.Tests` in the Editor, not only the Roslyn `.rsp` compile check.
2. Run HarbourDistrict and PoliceStation full matches through the actual UI/map-selection path.
3. Run at least one 2-human + 6-Bot session and confirm Bots can move, finish/repair tasks, report/vote, and reach settlement without deadlock.
4. Run true Relay sessions with Bots enabled and verify Host-only Bot decisions do not appear on clients as divergent state.
5. Collect the M8.4 target data set: at least 20 complete matches, including both maps and at least some human players.
6. Tune `OnlineRuleSet` from logged match data until 8-player matches land near 10-15 minutes and faction win rates are close to 45-55.
7. Migrate the remaining Netcode `ServerRpc(RequireOwnership=false)` calls to the newer `[Rpc]` API before release hardening.
